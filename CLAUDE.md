Skills live under `skills/<bucket>/<skill-name>/SKILL.md`.

Active bucket:

- `engineering/` — daily code work

Add new buckets only when needed. Every skill must have:

- An entry in `.claude-plugin/plugin.json` `skills[]`
- A row in the top-level `README.md` under the matching bucket section
- A row in the bucket's own `README.md` index, with the skill name linked to its `SKILL.md`
